name: agromapping
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Pacotes de HTTP e Estado
  dio: ^5.5.0+1
  provider: ^6.1.2
  shared_preferences: ^2.5.3
  
  # Pacotes para o Mapa (do projeto bflow)
  flutter_map: ^6.1.0
  geolocator: ^12.0.0
  permission_handler: ^11.3.1
  latlong2: ^0.9.1 

  # Pacotes de UI e Utilidades
  intl: ^0.19.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true